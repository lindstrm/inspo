services:
  inspo:
    build: .
    container_name: inspo
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
